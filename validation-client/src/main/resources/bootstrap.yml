spring:
  application:
     name: validation-client
    

 
server:
  port: 8886
  

management:
  security:
    enabled: false  
    
# Eureka properties
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8889/eureka     
    
terminal:
  server:
    name: terminal-server           
        